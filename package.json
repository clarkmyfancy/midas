{
  "name": "midas",
  "private": true,
  "packageManager": "pnpm@10.6.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "dev:site": "turbo run dev --parallel --filter @midas/backend --filter @midas/web",
    "memory:up": "sh scripts/start_memory_stack.sh",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "generate:types": "pnpm --filter @midas/backend generate:types"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.2"
  }
}
